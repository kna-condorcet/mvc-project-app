@model ProjectIndexViewModel

@{
    ViewData["Title"] = "Projects";
}

<div class="text-center">
    <h1 class="display-4">Projects</h1>
    <a class="text-dark" asp-area="" asp-controller="Projects" asp-action="Create">Create Project</a>
    <table class="table">
        <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].ExpectedEndDate)
            </th>
        </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Projects)
            {
                <tr>
                    <td>
                        <strong-text text="@item.Title"></strong-text>
                    </td>
                    <td>
                        @Html.DateFormat(item.ExpectedEndDate)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

